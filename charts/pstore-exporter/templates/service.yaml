apiVersion: v1
kind: Service
metadata:
  name: {{ include "pstore-exporter.fullname" . }}
  labels:
    {{- include "pstore-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pstore-exporter.selectorLabels" . | nindent 4 }}
