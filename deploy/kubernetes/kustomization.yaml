apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.example.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment if the Prometheus Operator (ServiceMonitor CRD) is installed:
  # - servicemonitor.yaml
