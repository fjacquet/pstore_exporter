apiVersion: apps/v1
kind: Deployment
metadata:
  name: pstore-exporter
  labels:
    app.kubernetes.io/name: pstore-exporter
spec:
  # Singleton collector: a second replica would double-poll every array
  # (no leader election). Recreate avoids two pods overlapping on rollout.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: pstore-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pstore-exporter
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pstore-exporter
          image: ghcr.io/fjacquet/pstore_exporter:0.1.0
          args: ["--config", "/etc/pstore_exporter/config.yaml"]
          ports:
            - name: metrics
              containerPort: 9446
          envFrom:
            - secretRef:
                name: pstore-exporter-secrets
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            # /metrics reflects process health (always 200 while serving).
            httpGet:
              path: /metrics
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            # /health is 503 until at least one array is collected.
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /etc/pstore_exporter
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: config
          configMap:
            name: pstore-exporter-config
        - name: tmp
          emptyDir: {}
