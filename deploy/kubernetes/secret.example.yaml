apiVersion: v1
kind: Secret
metadata:
  name: pstore-exporter-secrets
  labels:
    app.kubernetes.io/name: pstore-exporter
type: Opaque
stringData:
  # Referenced as ${PSTORE1_PASSWORD} in the ConfigMap's config.yaml.
  # Replace before applying; consider an external secret manager in production.
  PSTORE1_PASSWORD: change-me
